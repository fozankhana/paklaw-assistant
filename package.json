{
  "name": "paklaw-assistant",
  "version": "1.0.0",
  "description": "AI-powered chatbot and law library for the Constitution and legal Acts of Pakistan",
  "private": true,
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently -n server,client -c green,cyan \"npm run server\" \"npm run client\"",
    "start": "cd server && npm start",
    "validate": "cd server && npm run validate",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
